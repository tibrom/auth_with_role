mutation UpdateApiKeyUser($user_id: uuid, $api_key_hash: String = "") {
  update_users_user(_set: {api_key_hash: $api_key_hash}, where: {id: {_eq: $user_id}}) {
    user:returning {
      updated_at
    telegram_id
    username
    id
    email
    created_at
    password_hash
    aip_key_hash
    allowed_roles(order_by: {is_default: asc}) {
      created_at
      id
      is_default
      role
      user_id
    }
    }
  }
}
