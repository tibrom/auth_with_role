query CheckAuthMethodExists($identifier: String, $auth_type: String) {
  users_auth_method_aggregate(where: {_and: {identifier: {_eq: $identifier}, auth_type: {_eq: $auth_type}}}) {
    aggregate {
      count
    }
  }
}
